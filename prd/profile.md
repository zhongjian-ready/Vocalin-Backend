申请加入之后需要管理员同意，申请之后提示已发起申请，还没同意的时候，不能切换到刚刚状态为申请中的group，群组管理员登录进入之后右下角有消息的情况下增加一个消息浮球，上面显示一个消息的图标和消息数目。点击之后可以看到相应的消息list。在移交管理员的时候也有这样的逻辑，提示已发起移交，状态展示为移交中,被移交方登录之后有提示消息，同意之后再转换双方group角色。

在note页面有一个分类文件夹的概念，这个文件夹属于group中的个人，默认没有分类，放到all中，如果是group中其他人共享的public类型note，返回到其他成员则是属于shared类型。这里需要提供新建，编辑，删除这个分类的接口，其中all和shared不能编辑和删除。
